parm INPUT_PDB
reference INPUT_PDB
trajin INPUT_PDB

# Keep waters within CUTOFF Angstrom of residues RESIDUE_RANGE.
strip :WAT&!((:RESIDUE_RANGE)<:CUTOFF)

# Remove sodium and chloride ions after solvent stripping.
strip :Na+
strip :Cl-

trajout OUTPUT_PDB
